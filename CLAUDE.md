# signus

블라인드 신호 복조기 (PCM/IQ → 제원 자동탐지 → 복조 → 성상도). 무엇을/어떻게는 `README.md`,
파이프라인 단계와 정직한 한계까지 그쪽에 정리돼 있다. 명령어·테스트 절차·실신호 결과 보고
형식은 `docs/사용법-테스트-보고.md`.

## 기기가 셋이다 — 어디서 도는 코드인지 항상 확인할 것

| 기기 | 인터넷 | 코드가 들어가는 길 | Claude Code |
|---|---|---|---|
| 맥 | O | git | 돈다 |
| ssh 보드 | O | git | 돈다 |
| **실신호 장비** | **X** | **인쇄물 → 사람이 타이핑** | **안 돈다** |

**실신호 장비에는 `tools/` 가 없다.** 필사 대상은 `tools/codebook.py` 의 SECTIONS 뿐이고
(pyproject + `signus/*` 중 21개), 거기에 `tools/` 는 0건이다. 그러니 **그 장비가 써야 하는
기능은 반드시 `signus/` 안에 넣는다** — `tools/` 에 두면 그 도구부터 필사해야 한다.
단 `signus/` 라고 다 싣는 것은 아니다: 합성 생성기·채점(`signus/gen.py`, `signus/lab.py`)은
개발기 전용이라 **일부러 뺐다** (아래 인쇄물 절). 같은 이유로 그 장비에서는 pip 설치도,
인쇄물 발급도, `reports/` 커밋·푸시도 불가능하다.

## 실신호 결과가 `sig2 ...` 한 줄로 오면

그 장비에서 `signus analyze <파일> --brief` 가 발행한 줄이다. 끝에 오타 검출 코드가 붙는다.

```bash
echo "<받은 줄>" | tools/brief.py check      # 제일 먼저 이걸 돌린다
```

- **불일치면 고치기 전에 되묻는다.** 잘못 옮겨진 숫자로 수사를 시작하는 게 제일 비싼 사고다.
- 줄에는 코드 리비전이 없다. 그 장비 코드는 사용자가 손으로 친 것이라 믿을 리비전이 없고,
  지문 기능은 **필사 분량 때문에 일부러 뺐다**(2026-07-29 사용자 결정). 어느 인쇄본까지
  반영됐는지는 사용자에게 묻는다. **모든 줄이 불일치하면** 값이 아니라 `check_code` 필사를
  의심할 것 — 정상값은 `check_code("sig2 x an fs1 qpsk") == "psmx"` 다.
- 추가 정보가 필요하면 `docs/사용법-테스트-보고.md` §5.7 에서 **한 가지만** 고른다 — 한 글자도
  사용자가 손으로 친다.
- 재현 없이 추측으로 고치지 않는다.

## 깨뜨리면 안 되는 규칙

- **의존성은 numpy + scipy 단 둘.** 웹 UI 는 표준 라이브러리 서버 + CDN 요청 0 (격리망에서
  그대로 돌아가야 한다). numba 는 `[fast]` 선택 extra 이고, 없으면 동일 결과의 순수 numpy 로
  폴백해야 한다.
- **분석기는 정답 사이드카(`*.json` / `.sigmf-meta` 의 변조·fc·baud)를 읽지 않는다.**
  파일명·사이드카에서 읽어도 되는 것은 "읽기 필수 정보"(fs, iq/real, 샘플타입, RF 중심)뿐.
  정답은 채점기와 UI 비교 표시에만 쓴다.
- 실신호에서 발견한 실패는 합성으로 재현해 `tests/` 에 잠근다. `pytest -q`, ruff
  `line-length=100`.

## 필사용 인쇄물 (사용자가 손으로 따라 치는 종이)

사용자는 실신호 디버깅과 병행해 코드를 **손으로 필사**한다. 코드를 고쳤으면:

```bash
.venv/bin/python tools/codebook.py all --snap   # 재발급 + 기준점 갱신까지 한 번에 (docs/)
```

- **코드가 바뀐 턴에는 변경분(diff) 문서를 같이 발급한다.** 지난 인쇄본에서 고쳐야 할 줄만
  옛/새 줄번호와 함께 나오는 PDF다. 사용자가 한눈에 알아볼 수 있어야 한다.
- **기준점(snap)은 발급 직후 Claude 가 직접 갱신한다** (2026-08-02 사용자 지시 — 사용자에게
  바라지 않는다). 변경분은 그래서 항상 직전 변경 하나만 담는다. 사용자가 "몇 개 밀렸다"고
  하면 그때만 `diff --from git:<마지막으로 옮긴 리비전>` 으로 그 구간을 하나로 재발급한다.
- **테스트 코드는 인쇄물에 넣지 않는다.**
- **합성 신호 생성기와 채점 코드(`signus/gen.py`, `signus/lab.py`)도 넣지 않는다**
  (2026-08-03 사용자 지시). 그 코드는 개발기(Claude 가 도는 쪽)의 테스트·정합성 확인에만
  쓴다. 장비에서 `sweep` 자체 검증이 사라지는 대가로 필사 분량을 줄인 결정이다.
  발급기의 `EXCLUDED` 가 이를 강제하고, `signus/` 에 새 파일이 생기면 SECTIONS 나
  EXCLUDED 중 한쪽에 올려야 발급이 된다.
- **diff 의 코드 칸에 `+`/`−` 접두사를 붙이지 않는다** — 한 칸 밀리면 인덴트가 깨진다.
  표시는 줄번호 칸 색·볼드 + 코드 칸 밖의 부호 칸으로만.
- **바뀐 파일은 통째로 싣는다** (변경 구간만 뽑지 않음). 맨 앞 한두 줄에 변경/삭제된
  파일과 쪽번호를 적는다.
- **없어진 코드도, 그걸 설명하는 문구도 diff 에 싣지 않는다.** 사라진 자리는 붉은 가로
  점선으로만.
- **변경분은 표지 한 장으로 시작한다** (2026-08-02 사용자 지시로 승격): "바뀐 내용" 요약 +
  체크박스 파일 표(변경분·코드북 쪽번호). 요약은 발급할 때 `--note` 로 **알아듣기 좋은
  보통 문장**을 쓴다 — 커밋 문체(fix:/feat:) 금지, "무엇이 왜 바뀌었나"를 사용자 관점으로.
  본문은 여전히 종이를 아낀다: 파일마다 새 쪽에서 시작하지 않는다.
- PDF 는 헤드리스 크롬으로 그린다. 맥 기본 경로와 리눅스의 `chromium` 을 자동으로 찾고,
  없으면 `CHROME=<실행파일> .venv/bin/python tools/codebook.py all` 로 지정한다.
  발급기 의존성(pygments)은 `pip install -e '.[tools]'`.
- 전체 규약과 레이아웃 상수의 근거: `docs/필사-코드북-지침.md`

## 세션 동기화 (맥 <-> ssh 보드)

`origin` 에 닿는 기기끼리만이다. 실신호 장비는 이 루프 밖이다 (위 표).
`.claude/settings.json` 의 훅이 `tools/session-sync.sh` 를 부른다.

- **세션 시작** — `origin` 을 fetch 해서 fast-forward 만 한다. 갈라졌거나 작업본과 겹치면
  손대지 않고 알리기만 한다 (합치는 방식은 사람이 정한다).
- **세션 끝** — 남아 있는 변경을 `chore(wip)` 로 커밋하고 푸시한다. 그러니 **의미 있는 커밋은
  턴 안에서 직접 남긴다** — 세션 끝 커밋은 "다른 기기로 넘기는 임시 저장"이지 마무리가 아니다.
- **origin 은 공개 저장소다.** 실신호 캡처는 `.gitignore`(`caps/`, `*.iq`, `*.pcm`, `*.dat`,
  `*.sigmf-data`) 와 훅의 1MB 제동, 두 겹으로 막는다. 큰 파일이 끼면 자동 커밋을 아예 멈춘다.
- 오프라인·인증 실패면 **커밋은 하고 푸시만 실패**한다 (작업을 잃지 않기 위해서다).
  detached HEAD·원격 없음이면 아무것도 하지 않는다. 어느 쪽이든 세션은 멎지 않는다.
